#输入单个数字：
a = int(input().strip())
#输入单个字符串并转化为list：
b = list(input().strip())
#输入多个list
P = []
for i in range(n):
    P.append(list(map(int, input().strip().split())))
#创建字典：
dic = {}
idea = list()
if idea[i] not in dic:
    dic[idea[i]].append(idea)
#如果不确定输入的数量，使用：
while True:
    try:
    except:
        break
